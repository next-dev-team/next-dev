---
nav:
  title: Utils
toc: content
title: isArray
---


 `isArray` function is used to check whether a given value is an array.

## isArray

```tsx | pure
  const arr = [1, 2, 3];
  const checkIsArr = isArray(arr);
  // output: true
