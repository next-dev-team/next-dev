{
  "name": "@nextdev/monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:docs": "turbo dev --filter=@rnr/docs",
    "dev:showcase": "turbo dev --filter=@rnr/showcase --concurrency 32",
    "dev:showcase:android": "turbo dev:android --filter=@rnr/showcase --concurrency 32",
    "dev:showcase:web": "turbo dev:web --filter=@rnr/showcase --concurrency 32",
    "lint": "turbo lint",
    "test": "turbo test",
    "build": "turbo build",
    "build:showcase": "turbo build --filter=showcase",
    "build:docs": "turbo build --filter=docs",
    "clean": "turbo clean && rm -rf .turbo node_modules"
  },
  "devDependencies": {
    "@rnr/configs": "workspace:*",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14",
    "turbo": "^2.6.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "@babel/*",
        "expo-modules-*",
        "typescript"
      ]
    }
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "packageManager": "pnpm@9.1.4",
  "resolutions": {
    "lightningcss": "1.27.0"
  }
}
